# 开发日志

## 2026-08-27：项目基线与首个闭环

### 已完成

- 阅读并拆解考核约束、交付物和面试关注点。
- 确定采用 VS Code 扩展，而不是 CLI 或独立 IDE。
- 建立需求、用例、总体设计、界面原型、计划、测试、风险和追踪文档。
- 初始化 Git 仓库，按“文档基线 - 扩展骨架 - agent 核心”拆分真实提交。
- 完成 TypeScript 扩展骨架、设置项和 SecretStorage 录入命令。
- 完成 OpenAI-compatible 模型客户端和原生 tool calling 消息协议。
- 完成 agent 循环、工具错误回传、最大步骤和取消处理。
- 完成受工作区约束的文件列表、搜索、读取、精确替换、写入和命令工具。
- 完成侧边栏时间线、命令逐次审批、停止、变更列表和 Diff 入口。
- 创建 GitHub 公开仓库 `jycschool/localforge-agent`，推送完整提交历史并设置 `main` 为跟踪分支。

### 验证证据

- `pnpm run check` 通过。
- `pnpm test` 通过，当前 11 项测试覆盖 agent 直接完成、多轮工具调用、最大步骤、取消、无效工具参数、工作区工具、命令拒绝和路径边界。
- `pnpm run build` 通过，生成扩展 bundle。
- 原型 SVG 已在 Chromium 中渲染检查，主要区域、字体和颜色可读。

### 未完成与次日重点

- 在 Extension Development Host 中做真实交互测试。
- 增加 WorkspaceTools 的临时目录集成测试和命令超时测试。
- 选择真实模型和兼容接口做端到端运行。
- 建立演示项目并固化“测试失败后修复”的演示用例。

### 偏差

首日实现进度早于原计划，agent 核心已经进入扩展；但真实模型端到端验证仍是主要不确定项，不能因为单元测试通过而视为 M2 完成。
