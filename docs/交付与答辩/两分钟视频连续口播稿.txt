【00:00—00:20｜画面：保持当前完整三栏画面，不点击；鼠标从左上角 RepoForge 标志缓慢移到顶部项目名。】
大家好，这是我做的 RepoForge，中文名叫代码锻造。它能直接打开本地项目，帮我们读代码、改代码和跑测试。界面用 Electron，Agent 流程和本地工具都是自己实现的。

【00:20—00:34｜画面：指向顶部 order-pricing 项目名、ModelScope 模型和“Agent 已验证”状态。】
现在是 order-pricing 演示项目。录视频前我们已经让模型完整跑过一次，所以这里直接看结果，不再重复跑，也不用等模型。

【00:34—00:50｜画面：鼠标依次划过左侧项目树与本次变更、中间代码与运行输出、右侧 Agent 对话，再划过 Skill、Memory、发送清单和历史入口。】
左边是项目文件和本次改动，中间看源码、Diff 和测试输出，右边看模型的工作过程。另外还支持多模型、Skill、Memory、附件、发送清单和历史。发送任务前还可以先看发送清单，确认使用的模型、权限、Skill、附件和预计 Token，避免把不需要的内容发出去。

【00:50—01:02｜画面：指向右侧成果卡，依次停在“1 个文件 +12/-3”“6 项测试通过”“12 次·2 次失败”和 Token 四格。】
先看结果卡：只改一个文件，增加十二行、删除三行，六个测试全部通过。两次没成功的工具调用也照样保留。

【01:02—01:16｜画面：点击执行计划右侧“展开”，从上到下指过四个已完成步骤，然后点击“收起”。】
这次用计划模式，模型先列四步计划，我同意后才修改。四步是先跑测试、分析问题、修改并复测、最后总结，现在都完成了。

【01:16—01:39｜画面：先指向中间运行输出的红色“首次 2/6”，再移动到绿色“最终 6/6”；随后指向上方 shipping.mjs 中的 99、59 和两段输入校验。】
第一次测试六个通过两个、失败四个。模型只改 shipping 文件，修复九十九元和五十九元免邮边界，再加上金额和会员类型检查。重新测试后六个全过，退出码是零。

【01:39—01:53｜画面：点击成果卡中的“查看变更”，进入 shipping.mjs 双栏 Diff；先指旧代码，再指新代码和左侧唯一变更文件。】
再看 Diff，测试文件没动，改的只有实现文件。系统保存命令输出和修改前后的内容，恢复时也会检查，避免盖掉其他改动。

【01:53—结束｜画面：先指向“完整记录”或顶部“历史”，再回到成果卡和完整三栏画面，最后定格。】
最后，整个任务会保存在历史里，可以随时回看。回放只展示之前的记录，不会再调用模型或执行命令。这就是 RepoForge 修复代码的完整过程。这个项目的亮点，不只是让模型改代码，而是把计划审批、命令批准、真实测试、Diff 审查和文件恢复连成一个完整流程，让每一步都看得见、能控制、能验证。以上就是我的项目演示，欢迎各位老师提问和指教，谢谢大家。
