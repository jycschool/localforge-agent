# 归档说明

本目录保存已经完成评审并冻结的阶段性快照。归档文档用于证明当时的范围、估算、结论和项目状态，不承担“当前事实唯一来源”的职责。

维护规则：

- 不把后续功能、测试数量或计划状态反向写入旧快照。
- 只允许修复断链、错别字和明确的事实性勘误，并保留原评审日期。
- 当前需求看 `product/`，当前设计看 `architecture/`，当前计划和进度看 `process/`，当前验证口径看 `quality/`。
- 若以后产生新的冻结基线，使用日期或阶段命名新增文件，不覆盖既有快照。
