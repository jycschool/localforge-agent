{
  "name": "localforge-agent",
  "displayName": "LocalForge",
  "description": "A standalone, transparent and local-first coding agent desktop app.",
  "version": "0.1.0",
  "private": true,
  "publisher": "localforge",
  "license": "UNLICENSED",
  "main": "./dist/main.js",
  "scripts": {
    "build": "node esbuild.mjs",
    "start": "pnpm run build && electron .",
    "dev": "node esbuild.mjs --watch",
    "check": "tsc --noEmit",
    "test": "vitest run --dir tests",
    "verify": "pnpm run check && pnpm test && pnpm run build",
    "verify:delivery": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-delivery.ps1",
    "shortcut:create": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/create-desktop-shortcut.ps1",
    "package:win": "pnpm run build && node scripts/package-windows.mjs",
    "package:delivery": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/prepare-delivery.ps1"
  },
  "devDependencies": {
    "@electron/packager": "20.3.0",
    "@types/node": "^22.15.0",
    "electron": "^44.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "packageManager": "pnpm@11.19.0"
}
