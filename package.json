{
  "name": "localforge-agent",
  "displayName": "LocalForge",
  "description": "A standalone, transparent and local-first coding agent desktop app.",
  "version": "0.1.0",
  "private": true,
  "publisher": "localforge",
  "license": "UNLICENSED",
  "main": "./dist/main.js",
  "scripts": {
    "build": "node esbuild.mjs",
    "start": "pnpm run build && electron .",
    "dev": "node esbuild.mjs --watch",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "electron": "^44.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "packageManager": "pnpm@11.19.0"
}
